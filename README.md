# rizzui
This repository is a custom component registry for React, built on top of the [shadcn-ui/registry-template]. It provides an extensible set of UI components, hooks, and utilities, meant for easy integration into any React or Next.js project leveraging Tailwind CSS v4.
